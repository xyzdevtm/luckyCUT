# luckyCUT
# Lucky Scale Installer

یک نصب‌کنندهٔ برای نصب، کافی است مخزن را کلون کرده و اسکریپت را اجرا کنید.

## نصب
```bash
git clone https://github.com/[USERNAME]/lucky-scale-installer.git
cd lucky-scale-installer
chmod +x install.sh
./install.sh
